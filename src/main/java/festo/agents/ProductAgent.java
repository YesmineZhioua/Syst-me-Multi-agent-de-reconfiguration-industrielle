package festo.agents;

import jade.core.Agent;

public class ProductAgent extends Agent {
    protected void setup() {
        System.out.println("Product Agent démarré (version simplifiée)");
    }
}